// W3Schools, C Booleans, Real-Life Examples
#include <stdio.h>

int main() {
    int myAge = 25;
    int votingAge = 18;

    printf("%d", myAge >= votingAge);   // Returns 1 (true), meaning 25 years olds are allowed to vote!
    return 0;
}
