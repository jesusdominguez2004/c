// W3Schools, C Operators
#include <stdio.h>

int main() {
    // Logical Operators
    int x = 5;
    int y = 3;

    // Returns 1 (true) because one of the conditions are true 
    // (5 is greater than 3, but 5 is not less than 4)
    printf("%d", x > 3 || x < 4);
    return 0;
}
