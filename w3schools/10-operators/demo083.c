// W3Schools, C Operators
#include <stdio.h>

int main() {
    // Logical Operators
    int x = 5;
    int y = 3;

    // Returns 1 (true) because 5 is greater than 3 AND 5 is less than 10
    printf("%d", x > 3 && x < 10);
    return 0;
}
